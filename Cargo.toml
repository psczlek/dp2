[package]
name = "dp"
version = "0.0.3"
description = "Print files in hex and ascii"
license = "MIT"
edition = "2024"

[dependencies]

[profile.release]
opt-level = 3
debug = false
strip = true
lto = true
codegen-units = 1
